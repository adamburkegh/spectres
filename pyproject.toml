[project]
name = "spectres-corpus-tools"
version = "0.1.0"
description = "One-off tooling that builds the Marx's Ghost quote corpus (story/shared/marx-corpus/) from public-domain source text. Not required to build or play the game -- only used to regenerate story/shared/marx-quotes.txt."
requires-python = ">=3.11"
dependencies = []
